# Runtime dependencies for the FastAPI service image.
# Covers API, Excel ingest, single-image visual ingest, TOS upload, and Milvus search.
# Script-only tooling is intentionally not included in this image.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
httpx>=0.27.0

# Excel parsing and image processing.
openpyxl>=3.1.0
pillow>=10.0.0

# Volcengine TOS upload.
tos>=2.7.0

# Vector search.
pymilvus>=2.4.0

# FastAPI multipart uploads for Excel/image files.
python-multipart>=0.0.9
